events {}

http {
    upstream gateway {
        server gateway:8000;
    }
    server {
        listen 80;
        location / {
            proxy_pass http://gateway;
            proxy_read_timeout 120s;
            proxy_connect_timeout 120s;
            proxy_send_timeout 120s;

        }
    }
}